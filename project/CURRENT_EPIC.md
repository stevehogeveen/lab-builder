# Current Epic

Title: Product Team Beta Onboarding and Repository Discovery

## Objective

Make LabBuilder understandable to Project OS and safe for future product work by adding a minimal Project OS product structure and a factual repository map.

## Scope

- Add the minimum Product OS project structure inside LabBuilder.
- Create the first ready-queue task.
- Produce `docs/13-Repository-Map.md`.
- Document stack, folders, entry points, features, tests, commands, gaps, debt, and the next recommended task.

## Non-Goals

- Do not redesign Project OS from this repository.
- Do not implement product features.
- Do not run hardware, RAID, factory reset, rebuild, or destructive workflows.
- Do not merge or rewrite the newer `infra-config-portal` app from this onboarding task.

## Acceptance Criteria

- `project/PROJECT_OS_LINK.md` exists.
- `project/NOW.md` exists.
- `project/STATUS.md` exists.
- `project/CURRENT_EPIC.md` exists.
- `project/queue/README.md` exists.
- `project/queue/ready/TASK-000-repository-discovery.md` exists.
- `project/reports/` and `project/mail/` are represented in git.
- `docs/13-Repository-Map.md` exists and covers the required discovery sections.
